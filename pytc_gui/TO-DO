truncate white space from end of ##csv spew
relink AIC test to put spew in text box
make message box same width as plot
make so "New Session" actually works (or emove)
global variable not set after connector
